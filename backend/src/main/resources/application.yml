kafka:
  streams:
    application.id: "fraud-engine-cluster"
    bootstrap.servers: ${KAFKA_BOOTSTRAP:localhost:9092}
    application.server: "${APP_HOST:localhost}:${APP_PORT:8080}"
    num.standby.replicas: 1